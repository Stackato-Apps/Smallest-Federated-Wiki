name: fedwiki

framework:
    type: node

env:
    URL: http://fedwiki.stacka.to/

processes:
  web: (cd server/express; NODE_ENV=production ./bin/server.js -p $PORT -u "$URL")

mem: 256M

services:
  fedwiki-fs: filesystem

ignores: []
