name: fedwiki
framework:
  type: sinatra
  runtime: ruby19
processes:
  web: cd server/sinatra && bundle exec rackup -s thin -p $PORT
mem: 128M
min_version:
  client: 1.4.3
services:
  ${name}-fs: filesystem
hooks:
  pre-running:
    - ln -s $STACKATO_FILESYSTEM data

